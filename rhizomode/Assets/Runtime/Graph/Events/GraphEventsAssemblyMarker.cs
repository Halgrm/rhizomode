#nullable enable

namespace Rhizomode.Graph.Events
{
    /// <summary>
    /// Phase 1A placeholder for Rhizomode.Graph.Events asmdef (Plan v5.3).
    /// Phase 2 で GraphEventBus / GraphChangeSet (DTO/id) / GraphMutationScope を配置。
    /// id ベース通知で write 側を一本化。
    /// </summary>
    internal static class GraphEventsAssemblyMarker
    {
    }
}
